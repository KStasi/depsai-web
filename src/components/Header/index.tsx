import { Connect } from '@components/example';

export const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">Depsai</h1>
      <Connect />
    </header>
  );
};
